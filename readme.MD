# Welcome

## Instructions

### One command install
```
curl -sSL setup.maxc.codes | bash
```

IMPORTANT: ⚠️ Make sure to run this in your `Downloads` directory ⚠️

```python3 run.py```
or if that fails
```python run.py```
